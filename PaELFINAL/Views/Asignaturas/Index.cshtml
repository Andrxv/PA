@model IEnumerable<PaELFINAL.Models.Asignatura>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.area.cod_nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.prerequisito.NombreAsignatura)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NombreAsignatura)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.creditos)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.area.cod_nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.prerequisito.NombreAsignatura)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombreAsignatura)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.creditos)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.asignaturaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.asignaturaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.asignaturaID })
        </td>
    </tr>
}

</table>
